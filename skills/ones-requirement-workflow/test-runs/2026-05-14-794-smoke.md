# ONES Requirement Workflow Skill Smoke: #794

**Date:** 2026-05-14

## Prompt

使用 `#794` 这个需求进行 skill 测试。

## Expected Skill Behavior

- Trigger `ones-requirement-workflow`.
- Classify the input as a requirement-oriented intent.
- Do not use the `requirements` MCP server.
- Do not list bugs by default.
- Prefer ONES read-only MCP data sources.
- If work-item-domain tools are unavailable and document-domain tools cannot retrieve content, stop and report the missing capability instead of fabricating an archive.

## Available Tools Observed

Only the existing document-domain `getDocContent` MCP tools were available in this session:

- `get_doc`
- `search_docs`
- `get_doc_context`
- `get_doc_outline`
- `get_doc_section`
- `get_doc_chunks`

The work-item-domain tools described by the skill were not available yet:

- `resolve_requirement`
- `get_requirement_detail`
- `get_execution_tasks`
- `resolve_bug`
- `get_bug_detail`
- `get_bug_parent_requirement`
- `list_requirement_bugs`
- `get_task_messages`

## Actions

1. Loaded the project-local skill from `skills/ones-requirement-workflow/SKILL.md`.
2. Confirmed `docs/ones-workflow` did not exist before the run.
3. Called `get_doc` with:

```json
{
  "ref": "#794",
  "view": "both",
  "include_raw": false,
  "include_resources": true
}
```

Result:

```text
ONES request failed upstream. Try again later or verify network connectivity.
```

4. Called `search_docs` with:

```json
{
  "query": "794",
  "limit": 10
}
```

Result:

```text
Failed to discover the ONES API path. Check ONES_BASE_URL and network access.
```

## Result

This is a compliant blocked run.

The skill did not:

- use the `requirements` MCP server
- fetch or list bugs
- create a fake requirement archive
- infer requirement body from unavailable data
- proceed toward code changes

The run stopped because current MCP availability is insufficient for a real `#794` requirement sync. To make this scenario pass end-to-end, implement the work-item-domain MCP tools or restore document-domain ONES discovery/network access.

## Next Action

Use this smoke result as evidence that the skill boundary is correct, then implement the missing work-item-domain MCP tools before re-running `#794` as a full requirement archive test.

## Follow-up

The work-item-domain MCP tools were added after this smoke run:

- `resolve_requirement`
- `get_requirement_detail`
- `get_execution_tasks`
- `resolve_bug`
- `get_bug_detail`
- `get_bug_parent_requirement`
- `list_requirement_bugs`
- `get_task_messages`

Re-run this smoke as an end-to-end requirement archive test after configuring `ONES_TEAM_ID` or using a full ONES task URL with a team id.
